id: L5-mock-M02-q3-postmortem
schemaVersion: v2
atomId: M-03
qTags:
  - Q3
drills: ["Q1", "Q2", "Q3", "Q4"]
stage: 5
level: L5
type: WalkthroughCard
stem: |
  Postmortem for Mock M02 Q3 (read function, V2.0 shape). Why did the
  failed attempt below lose marks? What's the exact repair?
levelLabel: L5 · M-03 · Postmortem
fullCode: |-
  void read_desks(desk_data desk_list, int number_to_read) {
    int i;
    for (i = 0; i < number_to_read; i++) {
      cin >> desk_list[i].room_id;
    }
  }
  // Bug: param `desk_data desk_list` is a single struct value, not an array.
  //      Missing `&` and `[]` per Deakin V2.0 convention.
steps:
  - line: 1
    code: Change `desk_data desk_list` to `desk_data &desk_list[]`.
    annotation: Repair step 1
    atomIds: []
  - line: 2
    code: 'Add the two missing cin lines: `cin >> desk_list[i].d_id;` and `cin >> desk_list[i].number_of_screens;`.'
    annotation: Repair step 2
    atomIds: []
  - line: 3
    code: 'Verify signature matches V2.0 verbatim: `void read_desks(desk_data &desk_list[], int number_to_read)`.'
    annotation: Repair step 3
    atomIds: []
source:
  kind: v2
  ref: Test2-SIT102-practice-2026T1.txt + V2.0 verbatim — Mock M02 retro
commonMistakeIds:
  - CM-missing-amp-sig
status: NEW
createdBy: SA-L5-Mock
notes: |
  Mock M02 retro — Q3 repair drill. V2.0 shape: &desk_list[] + 3 cin lines.
authoringStatus: DRAFT
migratedFrom: PostmortemCard
