# =====================================================================
# L5 / Mock M04 Q3 POSTMORTEM
# Diff failed attempt vs canonical, surface repair steps.
# =====================================================================
id: "L5-mock-M04-q3-postmortem"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q3"]
stage: 5
level: "L5"
type: "PostmortemCard"

stem: |
  Postmortem for Mock M04 Q3 (read function). Why did the failed
  attempt below lose marks? What's the exact repair?

failedAttempt: |
  void read_employees(employee_data arr, int count) {
    int i;
    for (i = 0; i < count; i++) {
      cin >> arr[i].name;
    }
  }
  // Bug: arr passed by value (no [], no &). Caller's array unchanged.

diagnosis: |
  Two stacked errors: (1) parameter `employee_data arr` accepts
  a single struct by value, not an array; (2) even if a value-array
  were possible, mutations would not propagate back. The correct
  form is `employee_data arr[]` (decay-array, the Deakin convention)
  which auto-passes by-pointer to the underlying memory.

repairSteps:
  - "Change `employee_data arr` to `employee_data arr[]`."
  - "Verify compile + run shows mutations persist."
  - "Drill Q3 read-function template 5x."

preventionTip: |
  Q3 signature is always `void read_X(EntityName arr[], int count)` — drill the [] and the &-style.

explanation: |
  Postmortem cards diff your failed answer against the canonical
  answer, label the bug, and surface exactly what to repair. Per
  Mock retry policy §7.5: any 70-89 score injects 4 postmortem
  cards (one per Q) before the next mock attempt unlocks.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt + V2.0 — Mock M04 retro"

commonMistakeIds: ["CM-missing-amp-sig"]
status: "NEW"
createdBy: "SA-L5-Mock"
authoringStatus: "DRAFT"
notes: |
  Mock M04 retro — Q3 repair drill.
