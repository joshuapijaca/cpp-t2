# Auto-emitted CM immunization (FaultInjectionCard) for CM-hyphen-id
id: "L2-cmimm-CM-hyphen-id-spot"
schemaVersion: "v2"
atomId: "L-21"
qTags: ["Q2"]
stage: 4
level: "L2"
type: "FaultInjectionCard"

stem: |
  This Q2 struct field has a hyphen in its name. Why won't it compile?

brokenCode: |
  struct stat_double {
    double max-value;
    double mystery;
  };

bugLocations: [1]

fixedCode: |
  struct stat_double {
    double max_value;
    double mystery;
  };

bugCategory: 'hyphen in identifier (not allowed)'

keyChecks:
  - 'max_value'

explanation: |
  C++ identifiers may use letters, digits, and `_`. Hyphen `-` is the subtraction operator, not a name char. Use `_` instead.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-hyphen-id)"

commonMistakeIds: ["CM-hyphen-id"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
