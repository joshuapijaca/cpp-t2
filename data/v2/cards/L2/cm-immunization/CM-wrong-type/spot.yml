# Auto-emitted CM immunization (FaultInjectionCard) for CM-wrong-type
id: "L2-cmimm-CM-wrong-type-spot"
schemaVersion: "v2"
atomId: "L-21"
qTags: ["Q2"]
stage: 4
level: "L2"
type: "FaultInjectionCard"

stem: |
  This Q2 struct uses the wrong base type for its float-array field. Spot the bug and fix it.

brokenCode: |
  struct stat_double {
    int numbers[SIZE];
    double mystery;
  };

bugLocations: [1]

fixedCode: |
  struct stat_double {
    double numbers[SIZE];
    double mystery;
  };

bugCategory: 'wrong type for array field'

keyChecks:
  - 'double numbers[SIZE]'

explanation: |
  The struct is `stat_double` and the rubric requires a double array. `int numbers[SIZE]` would silently truncate any decimal inputs. Use `double numbers[SIZE]`.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-wrong-type)"

commonMistakeIds: ["CM-wrong-type"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
