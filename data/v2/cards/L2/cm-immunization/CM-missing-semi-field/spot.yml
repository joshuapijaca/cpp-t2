# Auto-emitted CM immunization (FaultInjectionCard) for CM-missing-semi-field
id: "L2-cmimm-CM-missing-semi-field-spot"
schemaVersion: "v2"
atomId: "L-21"
qTags: ["Q2"]
stage: 4
level: "L2"
type: "FaultInjectionCard"

stem: |
  This Q2 struct definition has a syntax error. Find the missing punctuation.

brokenCode: |
  struct stat_double {
    double numbers[SIZE]
    double mystery;
  };

bugLocations: [2]

fixedCode: |
  struct stat_double {
    double numbers[SIZE];
    double mystery;
  };

bugCategory: 'missing `;` after struct field'

keyChecks:
  - 'double numbers[SIZE];'

explanation: |
  Each field declaration in a struct ends with `;`. Missing one makes the next field run on into a parse error.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-missing-semi-field)"

commonMistakeIds: ["CM-missing-semi-field"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
