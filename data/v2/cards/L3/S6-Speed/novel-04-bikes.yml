# =====================================================================
# L3 / R-05 / SpeedDrillCard — novel timed read_bikes
# =====================================================================
id: "L3-R05-novel-04"
schemaVersion: "v2"
atomId: "R-05"
qTags: ["Q3"]
stage: 6
level: "L3"
type: "SpeedDrillCard"
stem: |
  TIMED (novel): write the full read_bikes function in 75 seconds.
  int gears, double weight_kg, double price.
prompt: "Novel timed: read_bikes."
canonicalAnswer: |
  void read_bikes(bike_data &bikes[], int number_to_read)
  {
    for (int i = 0; i < number_to_read; i++)
    {
      cout << "Gears: ";
      cin >> bikes[i].gears;
      cout << "Weight (kg): ";
      cin >> bikes[i].weight_kg;
      cout << "Price: ";
      cin >> bikes[i].price;
    }
  }
keyChecks:
  - "void read_bikes(bike_data &bikes[], int number_to_read)"
  - "for (int i = 0; i < number_to_read; i++)"
  - "cin >> bikes[i].gears;"
  - "cin >> bikes[i].weight_kg;"
  - "cin >> bikes[i].price;"
forbiddenTokens:
  - "while"
explanation: |
  Novel entity under time pressure. Skeleton is invariant.
source:
  kind: "v2"
  ref: "cpp-t2/docs/16_test2_specific_redesign_v2.md#PART V"
status: "NEW"
authoringStatus: "DRAFT"
createdBy: "SA-L3-Q3"
