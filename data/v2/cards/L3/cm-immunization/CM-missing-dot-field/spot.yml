# Auto-emitted CM immunization (FaultInjectionCard) for CM-missing-dot-field
id: "L3-cmimm-CM-missing-dot-field-spot"
schemaVersion: "v2"
atomId: "R-00"
qTags: ["Q3"]
stage: 4
level: "L3"
type: "FaultInjectionCard"

stem: |
  This Q3 read function fails to compile because a struct field access is malformed. Spot the bug.

brokenCode: |
  void read_data(stat_double &data) {
    for (int i = 0; i < SIZE; i++) {
      cin >> data numbers[i];
    }
  }

bugLocations: [3]

fixedCode: |
  void read_data(stat_double &data) {
    for (int i = 0; i < SIZE; i++) {
      cin >> data.numbers[i];
    }
  }

bugCategory: 'missing `.` between struct and field'

keyChecks:
  - 'data.numbers[i]'

explanation: |
  Field access uses `.` (or `->` for pointers). `data numbers[i]` is two tokens with nothing between them.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-missing-dot-field)"

commonMistakeIds: ["CM-missing-dot-field"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
