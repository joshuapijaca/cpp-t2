# Auto-emitted CM immunization (FaultInjectionCard) for CM-missing-bracket-sig
id: "L3-cmimm-CM-missing-bracket-sig-spot"
schemaVersion: "v2"
atomId: "R-00"
qTags: ["Q3"]
stage: 4
level: "L3"
type: "FaultInjectionCard"

stem: |
  This Q3 function signature is missing parentheses. Spot the bug.

brokenCode: |
  void read_data stat_double &data
  {
    for (int i = 0; i < SIZE; i++) cin >> data.numbers[i];
  }

bugLocations: [1]

fixedCode: |
  void read_data(stat_double &data)
  {
    for (int i = 0; i < SIZE; i++) cin >> data.numbers[i];
  }

bugCategory: 'missing `()` around parameter list'

keyChecks:
  - '(stat_double &data)'

explanation: |
  Function signatures wrap their parameter list in parens: `name(params)`. Without parens it isn't a function declaration at all.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-missing-bracket-sig)"

commonMistakeIds: ["CM-missing-bracket-sig"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
