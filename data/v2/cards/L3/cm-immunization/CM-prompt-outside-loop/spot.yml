# Auto-emitted CM immunization (FaultInjectionCard) for CM-prompt-outside-loop
id: "L3-cmimm-CM-prompt-outside-loop-spot"
schemaVersion: "v2"
atomId: "R-00"
qTags: ["Q3"]
stage: 4
level: "L3"
type: "FaultInjectionCard"

stem: |
  This Q3 read function prompts the user only ONCE for N values. Spot the bug.

brokenCode: |
  void read_data(stat_double &data)
  {
    cout << "Enter value: ";
    for (int i = 0; i < SIZE; i++) {
      cin >> data.numbers[i];
    }
  }

bugLocations: [3]

fixedCode: |
  void read_data(stat_double &data)
  {
    for (int i = 0; i < SIZE; i++) {
      cout << "Enter value: ";
      cin >> data.numbers[i];
    }
  }

bugCategory: 'prompt outside loop — only printed once'

keyChecks:
  - 'for (int i = 0;'

explanation: |
  The prompt belongs INSIDE the loop, before each cin. Moving the cout out shows the prompt once but reads SIZE values silently.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-prompt-outside-loop)"

commonMistakeIds: ["CM-prompt-outside-loop"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
