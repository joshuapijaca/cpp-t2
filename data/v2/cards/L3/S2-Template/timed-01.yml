# =====================================================================
# L3 / R-01 / SpeedDrillCard — timed type read_desks
# =====================================================================
id: "L3-R01-timed-01"
schemaVersion: "v2"
atomId: "R-01"
qTags: ["Q3"]
stage: 2
level: "L3"
type: "SpeedDrillCard"
stem: |
  Timed: type the full read_desks function in 90 seconds.
prompt: "Type the full read_desks function."
canonicalAnswer: |
  void read_desks(desk_data &desks[], int number_to_read)
  {
    for (int i = 0; i < number_to_read; i++)
    {
      cout << "Width: ";
      cin >> desks[i].width;
      cout << "Height: ";
      cin >> desks[i].height;
      cout << "Depth: ";
      cin >> desks[i].depth;
    }
  }
keyChecks:
  - "void read_desks"
  - "&desks[]"
  - "for (int i = 0; i < number_to_read; i++)"
  - "cin >> desks[i].width;"
explanation: |
  Speed drill: skeleton is fixed; muscle-memory the 12 lines.
source:
  kind: "v2"
  ref: "cpp-t2/docs/16_test2_specific_redesign_v2.md#PART V"
status: "NEW"
authoringStatus: "DRAFT"
createdBy: "SA-L3-Q3"
