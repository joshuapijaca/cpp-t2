id: "L5-wildcard-16-q4"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q4"]
drills: ["Q4"]
stage: 5
level: "L5"
type: "MainWriteCard"

stem: |
  S5.5 wildcard: Dynamic count main. Write the main.

prompt: |
  Write main: const MAX=100, employee_data arr[MAX], read count, read employees, print employees, return 0.

canonicalAnswer: |
  int main() {
    const int MAX = 100;
    employee_data arr[MAX];
    int count;
    cin >> count;
    read_employees(arr, count);
    print_employees(arr, count);
    return 0;
  }

keyChecks:
  - "const int MAX = 100;"
  - "int main()"
  - "return 0;"

forbiddenTokens: []

expectedTerminal: []

explanation: |
  Wildcard main: const MAX, array decl, read count, call read, call
  print, return 0. The 6-step canonical Q4 shape.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — S5.5 dynamic-count main"

commonMistakeIds: ["CM-missing-const", "CM-pass-MAX-not-count"]
status: "NEW"
createdBy: "SA-L5-Mock"
authoringStatus: "DRAFT"
notes: |
  Wildcard #16: Q4 Dynamic count main.
