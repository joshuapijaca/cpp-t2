id: L5-postmortem-07-off-by-one-exit
schemaVersion: v2
atomId: M-03
qTags:
  - Q1
drills: ["Q1"]
stage: 5
level: L5
type: WalkthroughCard
stem: >
  Q1 trace — student wrote `i <= SIZE` and tried to access `numbers[SIZE]` after the loop exits. Out-of-bounds read;
  trace shows a phantom 6th iteration. Read the diff.
levelLabel: L5 · M-03 · Postmortem
fullCode: |-
  void who_am_i(test_data &data) {
      data.mystery = 0.0;
      for (int i = 0; i < SIZE; i++) {
          data.mystery = data.mystery + data.numbers[i];
      }
  }
steps:
  - line: 1
    code: for (int i = 0; i < SIZE; i++) {   // <-- use `<`, not `<=`
    annotation: Repair step 1
    atomIds: []
  - line: 2
    code: '    data.mystery = data.mystery + data.numbers[i];'
    annotation: Repair step 2
    atomIds: []
  - line: 3
    code: '}'
    annotation: Repair step 3
    atomIds: []
  - line: 4
    code: // After loop, i == SIZE. numbers[i] is out-of-bounds — never index it post-loop.
    annotation: Repair step 4
    atomIds: []
source:
  kind: practice
  ref: Test2-SIT102-practice-2026T1.txt — Q1 loop bound off-by-one
commonMistakeIds:
  - CM-off-by-one-exit
status: NEW
createdBy: SA-L5-Postmortem-Rewrite
notes: |
  Postmortem #07 — Q1 / off-by-one exit (i <= SIZE).
authoringStatus: DRAFT
migratedFrom: PostmortemCard
