id: L5-postmortem-06-mutate-array
schemaVersion: v2
atomId: M-03
qTags:
  - Q1
drills: ["Q1"]
stage: 5
level: L5
type: WalkthroughCard
stem: >
  Q1 trace — student inverted the assignment direction: wrote `data.numbers[i] = data.mystery` instead of `data.mystery
  = data.numbers[i]`. The mystery accumulator never updated; the input array got clobbered. Read the diff.
levelLabel: L5 · M-03 · Postmortem
fullCode: |-
  void who_am_i(test_data &data) {
      data.mystery = data.numbers[0];
      for (int i = 1; i < SIZE; i++) {
          if (data.numbers[i] > data.mystery) {
              data.mystery = data.numbers[i];
          }
      }
  }
steps:
  - line: 1
    code: if (data.numbers[i] > data.mystery) {
    annotation: Repair step 1
    atomIds: []
  - line: 2
    code: '    data.mystery = data.numbers[i];   // mystery on LEFT (it''s the accumulator)'
    annotation: Repair step 2
    atomIds: []
  - line: 3
    code: '}'
    annotation: Repair step 3
    atomIds: []
source:
  kind: practice
  ref: Test2-SIT102-practice-2026T1.txt — Q1 assignment direction
commonMistakeIds:
  - CM-mutate-array
status: NEW
createdBy: SA-L5-Postmortem-Rewrite
notes: |
  Postmortem #06 — Q1 / inverted assignment direction.
authoringStatus: DRAFT
migratedFrom: PostmortemCard
