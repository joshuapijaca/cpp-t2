id: "L4-S2-fillslot-line-03"
schemaVersion: "v2"
atomId: "Q-01"
qTags: 
  - "Q4"
stage: 2
level: "L4"
type: "ClozeCard"
stem: "After array decl, fill the line that declares the count variable."
source: 
  kind: "v2"
  ref: "C++T2 spec §6.2 S2 FillSlot line"
commonMistakeIds: []
status: "NEW"
authoringStatus: "DRAFT"
createdBy: "SA-L4-Q4"
code: |
  int main()
  {
      const int MAX = 700;
      desk_data desks[MAX];
      int desk_num;
  
      cout << "How many desks? ";
      cin >> desk_num;
  
      read_desks(desks, desk_num);
  
      for (int i = 0; i < desk_num; i++)
      {
          cout << desks[i].room_id << ", ";
          cout << desks[i].d_id << ", ";
          cout << desks[i].number_of_screens << endl;
      }
  
      return 0;
  }
clozeSentence: "After array decl, fill the line that declares the count variable."
answer: "int desk_num;"
explanation: "Canonical line: `int desk_num;`."
