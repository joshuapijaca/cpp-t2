# Auto-emitted CM immunization (FaultInjectionCard) for CM-missing-include
id: "L4-cmimm-CM-missing-include-spot"
schemaVersion: "v2"
atomId: "Q-00"
qTags: ["Q4"]
stage: 4
level: "L4"
type: "FaultInjectionCard"

stem: |
  This Q4 program doesn't compile because a header is missing. Find what to add at the top.

brokenCode: |
  using namespace std;
  
  int main() {
    cout << "Hi";
    return 0;
  }

bugLocations: [1]

fixedCode: |
  #include <iostream>
  using namespace std;
  
  int main() {
    cout << "Hi";
    return 0;
  }

bugCategory: 'missing `#include <iostream>`'

keyChecks:
  - '#include <iostream>'

explanation: |
  cin / cout / endl all live in <iostream>. Without that include the compiler doesn't know what `cout` is.

source:
  kind: "v2"
  ref: "Test 2 V2.0 — common-mistake immunization (CM-missing-include)"

commonMistakeIds: ["CM-missing-include"]
status: "NEW"
createdBy: "QA-M13-fix"
notes: "Auto-emitted CM immunization card (FaultInjectionCard / SpotError)"
authoringStatus: "DRAFT"
