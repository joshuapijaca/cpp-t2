id: "CM-pass-MAX-not-count-trap-trace"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q4"]
stage: 4
level: "L4"
type: "DecomposeCard"

stem: |
  Trap-trace for CM-pass-MAX-not-count: Q4 pass MAX vs count.

code: |
  // Trap: Called `read_X(arr, MAX)` instead of `read_X(arr, count)`.
  read_X(arr, MAX);
  ;

question: |
  What goes wrong with the line above on Q4?

options:
  - label: "A"
    text: "Called `read_X(arr, MAX)` instead of `read_X(arr, count)`."
  - label: "B"
    text: "Compile error caught immediately."
  - label: "C"
    text: "Nothing — both forms are equivalent."
  - label: "D"
    text: "Runtime crash at the first iteration."

correctLabel: "A"

explanation: |
  Called `read_X(arr, MAX)` instead of `read_X(arr, count)`. The canonical pattern is: read_X(arr, count);

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake CM-pass-MAX-not-count"

commonMistakeIds: ["CM-pass-MAX-not-count"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 2/5 (trap-trace) for CM-pass-MAX-not-count.
