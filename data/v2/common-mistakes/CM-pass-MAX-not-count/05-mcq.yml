id: "CM-pass-MAX-not-count-mcq"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q4"]
stage: 4
level: "L4"
type: "MCQCard"

stem: |
  Rule recall for CM-pass-MAX-not-count: Q4 pass MAX vs count. Pass the runtime count, not the array capacity.

correct: "read_X(arr, count);"

distractors:
  - "read_X(arr, MAX);"
  - "// no init needed"
  - "// the compiler infers it"

explanation: |
  Canonical: read_X(arr, count);
  Mistake: Called `read_X(arr, MAX)` instead of `read_X(arr, count)`.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake CM-pass-MAX-not-count"

commonMistakeIds: ["CM-pass-MAX-not-count"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 5/5 (rule-recall MCQ) for CM-pass-MAX-not-count.
