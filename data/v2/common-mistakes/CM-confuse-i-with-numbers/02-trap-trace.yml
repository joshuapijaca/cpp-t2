id: "CM-confuse-i-with-numbers-trap-trace"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q1"]
stage: 4
level: "L4"
type: "DecomposeCard"

stem: |
  Trap-trace for CM-confuse-i-with-numbers: Q1 confused i with numbers[i].

code: |
  // Trap: Wrote `if (i > 5)` instead of `if (data.numbers[i] > 5)`.
  if (i > 5)
  ;

question: |
  What goes wrong with the line above on Q1?

options:
  - label: "A"
    text: "Wrote `if (i > 5)` instead of `if (data.numbers[i] > 5)`."
  - label: "B"
    text: "Compile error caught immediately."
  - label: "C"
    text: "Nothing — both forms are equivalent."
  - label: "D"
    text: "Runtime crash at the first iteration."

correctLabel: "A"

explanation: |
  Wrote `if (i > 5)` instead of `if (data.numbers[i] > 5)`. The canonical pattern is: if (data.numbers[i] > 5)

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake CM-confuse-i-with-numbers"

commonMistakeIds: ["CM-confuse-i-with-numbers"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 2/5 (trap-trace) for CM-confuse-i-with-numbers.
