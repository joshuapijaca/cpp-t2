id: "CM-skipped-field-cloze"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q3"]
stage: 4
level: "L4"
type: "ClozeCard"

stem: |
  Cloze for CM-skipped-field: Q3 skipped field in read. Fill the blank.

code: |
  // Canonical Q3 excerpt with blank:
  // ____ >> arr[i].brand; cin >> arr[i].year; cin >> arr[i].price;
  ;

clozeSentence: |
  Complete the canonical line: cin >> arr[i].brand; cin >> arr[i].year; cin >> arr[i].price;

answer: "cin >> arr[i].brand; cin >> arr[i].year; cin >> arr[i].price;"

explanation: |
  Canonical pattern: cin >> arr[i].brand; cin >> arr[i].year; cin >> arr[i].price;
  Mistake to avoid: Read 2 of 3 fields per iteration — misalignment.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake CM-skipped-field"

commonMistakeIds: ["CM-skipped-field"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 4/5 (cloze) for CM-skipped-field.
