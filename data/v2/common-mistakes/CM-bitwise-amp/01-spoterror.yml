id: "CM-bitwise-amp-spoterror"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q1"]
stage: 4
level: "L4"
type: "FaultInjectionCard"

stem: |
  Spot the error. Q1 bitwise & vs logical &&. The code below has the bug labeled
  by CM-bitwise-amp. Find the line and provide the fix.

brokenCode: |
  // Q1 broken excerpt:
  if (a > 0 & b > 0)
  // (canonical was: if (a > 0 && b > 0))
  ;

bugLocations: [1]

fixedCode: |
  if (a > 0 && b > 0)
  ;

bugCategory: "CM-bitwise-amp"

keyChecks:
  - "if (a > 0 && b > 0)"

explanation: |
  Bug: Used single & for logical AND.
  Fix: if (a > 0 && b > 0)
  This common mistake costs marks on Q1 per the rubric.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake catalogue CM-bitwise-amp"

commonMistakeIds: ["CM-bitwise-amp"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 1/5 (SpotError) for CM-bitwise-amp.
