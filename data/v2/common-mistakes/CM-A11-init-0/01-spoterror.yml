id: "CM-A11-init-0-spoterror"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q1"]
stage: 4
level: "L4"
type: "FaultInjectionCard"

stem: |
  Spot the error. Q1 init zero confusion. The code below has the bug labeled
  by CM-A11-init-0. Find the line and provide the fix.

brokenCode: |
  // Q1 broken excerpt:
  data.mystery = 0; // wrong for find-max
  // (canonical was: data.mystery = data.numbers[0]; // find-max)
  ;

bugLocations: [1]

fixedCode: |
  data.mystery = data.numbers[0]; // find-max
  ;

bugCategory: "CM-A11-init-0"

keyChecks:
  - "data.mystery = data.numbers[0]"

explanation: |
  Bug: Initialized find-max with 0 instead of numbers[0].
  Fix: data.mystery = data.numbers[0]; // find-max
  This common mistake costs marks on Q1 per the rubric.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake catalogue CM-A11-init-0"

commonMistakeIds: ["CM-A11-init-0"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 1/5 (SpotError) for CM-A11-init-0.
