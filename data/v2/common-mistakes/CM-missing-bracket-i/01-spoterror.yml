id: "CM-missing-bracket-i-spoterror"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q3"]
stage: 4
level: "L4"
type: "FaultInjectionCard"

stem: |
  Spot the error. Q3 missing [i] index. The code below has the bug labeled
  by CM-missing-bracket-i. Find the line and provide the fix.

brokenCode: |
  // Q3 broken excerpt:
  cin >> arr.field;
  // (canonical was: cin >> arr[i].field;)
  ;

bugLocations: [1]

fixedCode: |
  cin >> arr[i].field;
  ;

bugCategory: "CM-missing-bracket-i"

keyChecks:
  - "cin >> arr[i].field;"

explanation: |
  Bug: Wrote `cin >> arr.field` — missing the [i].
  Fix: cin >> arr[i].field;
  This common mistake costs marks on Q3 per the rubric.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake catalogue CM-missing-bracket-i"

commonMistakeIds: ["CM-missing-bracket-i"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 1/5 (SpotError) for CM-missing-bracket-i.
