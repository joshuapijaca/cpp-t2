id: "CM-iplusplus-before-body-spoterror"
schemaVersion: "v2"
atomId: "M-03"
qTags: ["Q1"]
stage: 4
level: "L4"
type: "FaultInjectionCard"

stem: |
  Spot the error. Q1 i++ before body trace. The code below has the bug labeled
  by CM-iplusplus-before-body. Find the line and provide the fix.

brokenCode: |
  // Q1 broken excerpt:
  // init -> i++ -> cond -> body -> ...
  // (canonical was: // init -> cond -> body -> i++ -> cond -> body -> ...)
  ;

bugLocations: [1]

fixedCode: |
  // init -> cond -> body -> i++ -> cond -> body -> ...
  ;

bugCategory: "CM-iplusplus-before-body"

keyChecks:
  - "// init -> cond -> body -> i++"

explanation: |
  Bug: Trace processes elements 1..5 instead of 0..4.
  Fix: // init -> cond -> body -> i++ -> cond -> body -> ...
  This common mistake costs marks on Q1 per the rubric.

source:
  kind: "practice"
  ref: "Test2-SIT102-practice-2026T1.txt — common-mistake catalogue CM-iplusplus-before-body"

commonMistakeIds: ["CM-iplusplus-before-body"]
status: "NEW"
createdBy: "SA-CM-Immune"
authoringStatus: "DRAFT"
notes: |
  CM card 1/5 (SpotError) for CM-iplusplus-before-body.
