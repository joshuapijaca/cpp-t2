node_modules/
dist/
# data/cards.json  # now tracked — 1,775 cards including code-centric L12-L17
.env
.env.local
*.log
.DS_Store
.vite/


# v2 build outputs (regenerable)
dist-v2/
coverage/
test-results/
playwright-report/

# Background test artifacts
*.tmp

# Lint reports (regenerable via npm run lint:*)
build-v2/*-lint-report.json

# vite-plugin-pwa dev output (regenerable; emits sw.js, registerSW.js, manifest stub on dev start)
dev-dist/
# Built service-worker artifact (lives in dist-v2/ but listed as a safety net
# in case anyone moves the build output up a level).
sw.js
workbox-*.js
