id: MW-08
fact: |-
  printf("%d %s
  ", id, name);
words: 5
level: 16
deps:
  - O-08
  - O-09
  - O-10
q_tags:
  Q1: 'N'
  Q2: 'N'
  Q3: 'N'
  Q4: C
pfg_source: []
test2_evidence: []
canonical_example: ''
expected_output: ''
sit102_quirks: []
misconceptions: []
render_hints:
  memorize_seed_phrases:
    - printf %d %s with values
    - format string then args
    - printf format matches types
    - '%d for int %s for string'
    - one printf per record
  see_demo:
    why_one_line: printf with format string + .c_str() prints each record on one line.
    snippet: |
      printf("%d %s\n", list[i].id, list[i].title.c_str());
    highlights:
      - '%d %s\n'
      - .c_str()
    used_in:
      - Q4
  see_decompose:
    snippet: |-
      int main() {
        const int MAX = 100;
        book list[MAX];
        int count;
        cin >> count;
        read_book(list, count);
        for (int i = 0; i < count; i++) {
          printf("%d %s %.2f\n", list[i].id, list[i].title.c_str(), list[i].price);
        }
        return 0;
      }
    correct_atoms:
      - MW-08
      - O-08
      - O-09
      - O-10
    distractors:
      - MW-01
      - MW-02
      - R-01
acceptance:
  memorize:
    - <=7 words
lint:
  miller_max_words: 7
status: locked
