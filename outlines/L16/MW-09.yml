id: MW-09
fact: .c_str() converts string for %s
words: 5
level: 16
deps:
  - O-10
q_tags:
  Q1: 'N'
  Q2: 'N'
  Q3: 'N'
  Q4: C
pfg_source: []
test2_evidence: []
canonical_example: ''
expected_output: ''
sit102_quirks: []
misconceptions: []
render_hints:
  memorize_seed_phrases:
    - .c_str() gets C-string
    - name.c_str() for printf %s
    - string needs c_str() for %s
    - convert C++ string to char*
    - use .c_str() with %s
  see_demo:
    why_one_line: C++ string -> %s needs .c_str() ; without it printf prints garbage.
    snippet: |
      string name = "ada";
      printf("%s\n", name.c_str());   // C-string for %s
    highlights:
      - .c_str()
    used_in:
      - Q4
  see_decompose:
    snippet: |-
      int main() {
        const int MAX = 100;
        book list[MAX];
        int count;
        cin >> count;
        read_book(list, count);
        for (int i = 0; i < count; i++) {
          printf("%d %s %.2f\n", list[i].id, list[i].title.c_str(), list[i].price);
        }
        return 0;
      }
    correct_atoms:
      - MW-09
      - O-10
    distractors:
      - MW-01
      - MW-02
      - R-01
acceptance:
  memorize:
    - <=7 words
lint:
  forbid_tokens:
    - cout
    - std::string
  miller_max_words: 7
status: locked
