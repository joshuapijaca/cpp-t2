id: MW-06
fact: call read_X(list, count);
words: 3
level: 16
deps:
  - MW-03
  - MW-05
q_tags:
  Q1: 'N'
  Q2: 'N'
  Q3: 'N'
  Q4: C
pfg_source: []
test2_evidence: []
canonical_example: ''
expected_output: ''
sit102_quirks: []
misconceptions: []
render_hints:
  memorize_seed_phrases:
    - call read_X(list, count)
    - pass list and count
    - invoke read function
    - read_X fills the array
    - no & at call site
  see_demo:
    why_one_line: call read_X(list, count); to fill the array via Q3's function.
    snippet: |
      read_book(list, count);
    highlights:
      - read_book(list, count);
    used_in:
      - Q4
  see_decompose:
    snippet: |-
      int main() {
        const int MAX = 100;
        book list[MAX];
        int count;
        cin >> count;
        read_book(list, count);
        for (int i = 0; i < count; i++) {
          printf("%d %s %.2f\n", list[i].id, list[i].title.c_str(), list[i].price);
        }
        return 0;
      }
    correct_atoms:
      - MW-06
      - MW-03
      - MW-05
    distractors:
      - MW-01
      - MW-02
      - R-01
acceptance:
  memorize:
    - <=7 words
lint:
  miller_max_words: 7
status: locked
