id: WT-L16
level: 16
level_label: "L16 main-write -- Q4 full main"
anchor_atom_id: MW-01
status: locked
full_code: |
  int main() {
    const int MAX = 100;
    book list[MAX];
    int count;
    cin >> count;
    read_book(list, count);
    for (int i = 0; i < count; i++) {
      printf("%d %s %.2f\n", list[i].id, list[i].title.c_str(), list[i].price);
    }
    return 0;
  }
steps:
  - line: 1
    code: "int main() {"
    annotation: "Q4 always asks for a complete main."
    atom_ids: [MW-01, S-03]
  - line: 2
    code: "  const int MAX = 100;"
    annotation: "capacity constant; declared FIRST."
    atom_ids: [MW-02]
  - line: 3
    code: "  book list[MAX];"
    annotation: "array of structs sized to MAX."
    atom_ids: [MW-03, T-11]
  - line: 4
    code: "  int count;"
    annotation: "storage for actual record count."
    atom_ids: [MW-04]
  - line: 5
    code: "  cin >> count;"
    annotation: "user types record count."
    atom_ids: [MW-05, I-03]
  - line: 6
    code: "  read_book(list, count);"
    annotation: "call Q3 to fill the array."
    atom_ids: [MW-06]
  - line: 7
    code: "  for (int i = 0; i < count; i++) {"
    annotation: "print loop walks the records."
    atom_ids: [MW-07, W-03]
  - line: 8
    code: "    printf(\"%d %s %.2f\\n\", list[i].id, list[i].title.c_str(), list[i].price);"
    annotation: "printf with %d, %s, %.2f, .c_str() for the string."
    atom_ids: [MW-08, MW-09, O-08, O-09, O-10]
  - line: 10
    code: "  return 0;"
    annotation: "main ends with success."
    atom_ids: [S-07]
