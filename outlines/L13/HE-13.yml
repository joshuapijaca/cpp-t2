id: HE-13
fact: "trace if/else branches"
words: 3
level: 13
deps:
  - HE-05

q_tags:
  Q1: C
  Q2: N
  Q3: N
  Q4: N

pfg_source: []

test2_evidence:
  - question: Q1
    instance: "if (data.numbers[i] > data.mystery) - body taken when current element exceeds running max"
    file_path: extraction/test2_bank/Q1_who_am_i_variants.md

canonical_example: |
  int n = 7;
  int label;
  if (n > 5) {
      label = 1;
  } else {
      label = 2;
  }
  // final: label = 1
expected_output: "1"

sit102_quirks:
  - "One body runs; the other is skipped"
  - "Inline cond viz: 'n > 5 -> true'"

misconceptions:
  - id: M01
    description: "Both branches trace"
    student_says: "I traced both if and else"
    correct_says: "Only the matching branch executes"

render_hints:
  memorize_seed_phrases:
    - "one branch runs not both"
    - "true picks if body"
    - "false picks else body"
    - "skipped branch ignored"
    - "trace only taken branch"

  see_demo:
    why_one_line: "branch traces commit to one side based on the live condition value."
    snippet: |
      int n = 7;
      int label;
      if (n > 5) {
          label = 1;
      } else {
          label = 2;
      }
      // final: label = 1
    highlights: []
    used_in: [Q1]
  see_decompose:
    snippet: |
      int n = 7;
    correct_atoms: [HE-13, HE-05]
    distractors: [HE-01, HE-10, R-01]
acceptance:
  memorize:
    - "Each variant <=7 words"
    - "Mentions: branch / if / else / pick"

lint:
  forbid_tokens: ["IDE", "debugger"]
  miller_max_words: 7

status: locked
locked_at: 2026-05-04
locked_by: M4
revision_history: []
